extends layout

block content
  h1 #{__('app-name')}
  p Welcome to #{title}
  input(type="text", ng-model="text")
  p {{text}}
  p #{message}
  p #{__('app-name')}

  ul
  each val in users 
    li= val.Login